server:
  port: 8006
spring:
  application:
    name: cloud-provider-payment8006
  cloud:
    consul:
      discovery:
        service-name: ${spring.application.name}
      host: localhost
      port: 8500